server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/metadata-service
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
